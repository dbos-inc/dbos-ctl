# Built binary (root-anchored so it doesn't match the cmd/dbosctl/ source dir).
/dbosctl
# goreleaser output.
/dist/
openapi.json

# Local test configuration — holds the conductor license key. Never commit.
# See .env.example.
.env

# Editor settings are personal — not committed.
.vscode/

# JUnit reports from `make test JUNIT=...`.
*-tests.xml
