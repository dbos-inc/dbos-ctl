# Built binary (root-anchored so it doesn't match the cmd/dbos/ source dir).
/dbos
# goreleaser output.
/dist/
openapi.json

# Local test configuration — holds the conductor license key. Never commit.
# See .env.example.
.env

# Editor settings are personal — not committed.
.vscode/
